version: "3.9"
services:
  dashboard:
    container_name: dashboard
    image: hummingbot/dashboard:latest
    volumes:
      - ./hummingbot_files/data:/home/dashboard/data
    ports:
      - "8501:8501"
